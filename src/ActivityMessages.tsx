import {useEffect,useState} from 'react';
import {api,Message,User} from './api';
import {ago,A,Empty} from './SocialShared';
export function Activity({message}:{message:(u:User,c?:string)=>void}){const[data,setData]=useState<any[]>([]);useEffect(()=>{api.notifications().then(r=>setData(r.notifications||[])).catch(()=>{})},[]);return <section><div className="title"><div><small>Stay in the loop</small><h1>Activity</h1></div><button onClick={()=>api.readAllNotifications()}>Mark read</button></div>{!data.length?<Empty title="You're all caught up" text="New follows, likes, replies and messages will appear here."/>:<div>{data.map((n,i)=><button className="activity" key={n.id||i} onClick={()=>{if(n.id)api.readNotification(n.id);if(n.type==='message'&&n.actor_id)message({id:n.actor_id,username:n.actor_username||'user',email:''},n.entity_id||n.reference_id||'')}}><A u={n.actor}/><span><b>{n.message||`${n.actor_username||'Someone'} ${n.type||'activity'}`}</b><small>{ago(n.created_at)}</small></span></button>)}</div>}</section>}
export function Messages({me,target,cid,open}:{me:User;target:User|null;cid:string;open:(u:User,c?:string)=>void}){const[selected,setSelected]=useState(target),[conversation,setConversation]=useState(cid),[messages,setMessages]=useState<Message[]>([]),[q,setQ]=useState(''),[results,setResults]=useState<User[]>([]),[draft,setDraft]=useState('');useEffect(()=>{setSelected(target);setConversation(cid)},[target,cid]);useEffect(()=>{if(!selected)return;let live=true;(async()=>{let c=conversation;if(!c)c=(await api.conversationWith(selected.id)).conversation_id||'';if(c){setConversation(c);const r=await api.messages(c);if(live)setMessages(r.messages||[]);api.readMessages(c).catch(()=>{})}else if(live)setMessages([])})();return()=>{live=false}},[selected,conversation]);const search=async()=>{if(q.length<2)return;try{setResults((await api.search(q,20)).users||[])}catch{setResults([])}};const send=async()=>{if(!selected||!draft.trim())return;try{const r=await api.sendMessage(selected.id,draft.trim());setConversation(r.conversation_id);setDraft('');setMessages((await api.messages(r.conversation_id)).messages||[])}catch(e){alert(e instanceof Error?e.message:'Unable to send')}};return <section className="inbox"><div className="title"><div><small>Private conversations</small><h1>Messages</h1></div></div><div className="msg-search"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()} placeholder="Search people…"/><button onClick={search}>⌕</button></div><div className="inbox-grid"><aside>{results.map(u=><button key={u.id} onClick={()=>{setSelected(u);setResults([]);open(u,'')}}><A u={u}/>@{u.username}</button>)}{!results.length&&<p>Search someone to start a chat.</p>}</aside><div className="chat">{selected?<><div className="chat-head"><A u={selected}/><b>@{selected.username}</b></div><div className="messages">{messages.length?messages.map((m,i)=>{const mine=String(m.sender_id||'')===me.id;return <div className={mine?'bubble mine':'bubble'} key={m.id||m.message_id||i}>{m.content||m.body}<small>{ago(m.created_at)}</small></div>}):<Empty title="Start the conversation" text="Say hello and see where it goes."/>}</div><div className="send"><textarea value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder={`Message @${selected.username}…`}/><button onClick={send}>➤</button></div></>:<Empty title="Choose a conversation" text="Search for a person and open a private chat."/>}</div></div></section>}
