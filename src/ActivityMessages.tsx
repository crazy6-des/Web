import {useEffect,useState} from 'react';
import {api,Message,User,ConversationSummary} from './api';
import {ago,A,Empty} from './SocialShared';
export function Activity({message}:{message:(u:User,c?:string)=>void}){const[data,setData]=useState<any[]>([]);useEffect(()=>{api.notifications().then(r=>setData(r.notifications||[])).catch(()=>{})},[]);return <section><div className="title"><div><small>Stay in the loop</small><h1>Activity</h1></div><button onClick={async()=>{await api.readAllNotifications();setData(x=>x.map(n=>({...n,read:true})))}}>Mark read</button></div>{!data.length?<Empty title="You're all caught up" text="New follows, likes, replies and messages will appear here."/>:<div>{data.map((n,i)=><button className="activity" key={n.id||i} onClick={()=>{if(n.id)api.readNotification(n.id);if(n.type==='message'&&n.actor_id)message({id:n.actor_id,username:n.actor_username||'user',email:''},n.entity_id||n.reference_id||'')}}><A u={n.actor}/><span><b>{n.message||`${n.actor_username||'Someone'} ${n.type||'activity'}`}</b><small>{ago(n.created_at)}</small></span></button>)}</div>}</section>}
export function Messages({me,target,cid,open}:{me:User;target:User|null;cid:string;open:(u:User,c?:string)=>void}){const[selected,setSelected]=useState(target),[conversation,setConversation]=useState(cid),[messages,setMessages]=useState<Message[]>([]),[inbox,setInbox]=useState<ConversationSummary[]>([]),[q,setQ]=useState(''),[results,setResults]=useState<User[]>([]),[draft,setDraft]=useState('');const refreshInbox=()=>api.inbox().then(r=>setInbox(r.conversations||[])).catch(()=>{});useEffect(()=>{setSelected(target);setConversation(cid)},[target,cid]);useEffect(()=>{refreshInbox();const timer=window.setInterval(refreshInbox,5000);return()=>window.clearInterval(timer)},[]);useEffect(()=>{if(!selected)return;let live=true;(async()=>{let c=conversation;if(!c)c=(await api.conversationWith(selected.id)).conversation_id||'';if(c){setConversation(c);const r=await api.messages(c);if(live)setMessages(r.messages||[]);api.readMessages(c).catch(()=>{})}else if(live)setMessages([])})();return()=>{live=false}},[selected,conversation]);useEffect(()=>{if(!conversation)return;const timer=window.setInterval(async()=>{try{const r=await api.messages(conversation);setMessages(r.messages||[])}catch{}},3000);return()=>window.clearInterval(timer)},[conversation]);const search=async()=>{if(q.length<2)return;try{setResults((await api.search(q,20)).users||[])}catch{setResults([])}};const openConversation=(u:User,c:string)=>{setSelected(u);setConversation(c);setResults([]);open(u,c)};const send=async()=>{if(!selected||!draft.trim())return;try{const r=await api.sendMessage(selected.id,draft.trim());setConversation(r.conversation_id);setDraft('');setMessages((await api.messages(r.conversation_id)).messages||[]);refreshInbox()}catch(e){alert(e instanceof Error?e.message:'Unable to send')}};return <section className="inbox"><div className="title"><div><small>Private conversations</small><h1>Messages</h1></div></div><div className="msg-search"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()} placeholder="Search people…"/><button onClick={search}>⌕</button></div><div className="inbox-grid"><aside>{inbox.map(c=><button key={c.conversation_id} className={conversation===c.conversation_id?'on':''} onClick={()=>openConversation(c.user,c.conversation_id)}><A u={c.user}/><span><b>@{c.user.username}</b><small>{c.lastMessage||'Conversation'}</small></span>{c.unread>0&&<em>{c.unread}</em>}</button>)}{results.map(u=><button key={`search-${u.id}`} onClick={()=>openConversation(u,'')}><A u={u}/><span>@{u.username}<small>Start or continue conversation</small></span></button>)}{!inbox.length&&!results.length&&<p>No conversations yet. Search someone to start one.</p>}</aside><div className="chat">{selected?<><div className="chat-head"><A u={selected}/><b>@{selected.username}</b></div><div className="messages">{messages.length?messages.map((m,i)=>{const mine=String(m.sender_id||m.user_id||'')===me.id;return <div className={mine?'bubble mine':'bubble'} key={m.id||m.message_id||i}>{m.content||m.body}<small>{ago(m.created_at)}</small></div>}):<Empty title="Start the conversation" text="Say hello and see where it goes."/>}</div><div className="send"><textarea value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder={`Message @${selected.username}…`}/><button onClick={send}>➤</button></div></>:<Empty title="Choose a conversation" text="Your message history will appear here."/>}</div></div></section>}
